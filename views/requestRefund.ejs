<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Request Refund - HB Mart</title>
    <link href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <!-- [NEW FILE] Created for refund request feature -->
    <style>
        .refund-container {
            max-width: 700px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        .refund-card {
            background: var(--white);
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
        }
        .order-summary {
            background: var(--light-gray);
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 25px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
        }
        .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid var(--light-gray);
            border-radius: 6px;
            font-size: 14px;
        }
        .form-group textarea {
            min-height: 100px;
            resize: vertical;
        }
        .reason-option {
            display: flex;
            align-items: center;
            padding: 15px;
            border: 2px solid var(--light-gray);
            border-radius: 8px;
            margin-bottom: 10px;
            cursor: pointer;
            transition: border-color 0.3s;
        }
        .reason-option:hover {
            border-color: var(--primary);
        }
        .reason-option input {
            margin-right: 15px;
        }
        .reason-option.selected {
            border-color: var(--primary);
            background: rgba(52, 152, 219, 0.05);
        }
        .refund-amount {
            font-size: 28px;
            font-weight: bold;
            color: var(--primary);
        }
        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffc107;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar', { user: user, currentPage: 'orders' }) %>

    <div class="refund-container">
        <div style="margin-bottom: 20px;">
            <a href="/order/<%= order.id %>" style="color: var(--primary); text-decoration: none;">
                <i class="fas fa-arrow-left"></i> Back to Order #<%= order.id %>
            </a>
        </div>

        <h1 style="margin-bottom: 30px;">
            <i class="fas fa-undo"></i> Request Refund
        </h1>

        <% if (messages.error && messages.error.length > 0) { %>
            <div class="alert alert-error"><%= messages.error[0] %></div>
        <% } %>

        <div class="refund-card">
            <div class="order-summary">
                <h4 style="margin-bottom: 15px;">Order #<%= order.id %></h4>
                <%
                    const subtotal = parseFloat(order.total) || 0;
                    const deliveryFee = subtotal >= 60 ? 0 : 5;
                    const discount = parseFloat(order.discount_amount) || 0;
                    const refundAmount = subtotal + deliveryFee - discount;
                %>
                <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
                    <span>Order Total:</span>
                    <span>$<%= subtotal.toFixed(2) %></span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
                    <span>Delivery Fee:</span>
                    <span>$<%= deliveryFee.toFixed(2) %></span>
                </div>
                <% if (discount > 0) { %>
                    <div style="display: flex; justify-content: space-between; margin-bottom: 10px; color: #28a745;">
                        <span>Discount:</span>
                        <span>-$<%= discount.toFixed(2) %></span>
                    </div>
                <% } %>
                <div style="display: flex; justify-content: space-between; padding-top: 10px; border-top: 2px solid var(--medium-gray);">
                    <span style="font-weight: 600;">Refund Amount:</span>
                    <span class="refund-amount">$<%= refundAmount.toFixed(2) %></span>
                </div>
            </div>

            <div class="warning-box">
                <i class="fas fa-exclamation-triangle"></i>
                <strong>Please note:</strong> Refund requests are subject to approval. Once approved, the refund will be processed within 3-5 business days.
            </div>

            <form action="/order/<%= order.id %>/refund" method="POST">
                <div class="form-group">
                    <label>Select a reason for refund *</label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Damaged" required>
                        <div>
                            <strong>Damaged Product</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">Product arrived damaged or broken</div>
                        </div>
                    </label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Wrong Item">
                        <div>
                            <strong>Wrong Item</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">Received wrong item(s)</div>
                        </div>
                    </label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Not as Described">
                        <div>
                            <strong>Not as Described</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">Product doesn't match description</div>
                        </div>
                    </label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Changed Mind">
                        <div>
                            <strong>Changed Mind</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">No longer want the product</div>
                        </div>
                    </label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Late Delivery">
                        <div>
                            <strong>Late Delivery</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">Order arrived too late</div>
                        </div>
                    </label>
                    <label class="reason-option" onclick="selectReason(this)">
                        <input type="radio" name="reason" value="Other">
                        <div>
                            <strong>Other</strong>
                            <div style="font-size: 13px; color: var(--dark-gray);">Other reason (please specify below)</div>
                        </div>
                    </label>
                </div>

                <div class="form-group">
                    <label for="description">Additional Details (optional)</label>
                    <textarea name="description" id="description" placeholder="Please provide any additional details about your refund request..."></textarea>
                </div>

                <div style="display: flex; gap: 15px;">
                    <a href="/order/<%= order.id %>" class="btn btn-secondary" style="flex: 1; text-align: center;">Cancel</a>
                    <button type="submit" class="btn" style="flex: 1;">
                        <i class="fas fa-paper-plane"></i> Submit Request
                    </button>
                </div>
            </form>
        </div>
    </div>

    <%- include('partials/footer', { user: user }) %>

    <script>
        function selectReason(el) {
            document.querySelectorAll('.reason-option').forEach(opt => opt.classList.remove('selected'));
            el.classList.add('selected');
        }
    </script>
</body>
</html>
